server:
  port: 9999
spring:
  application:
    name: gateway-server
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
#          routes:
#            - id: customer-service
#              uri: lb://CUSTOMER:20261
#              predicates:
#                - Path=/api/v1/customers/**


#            - id: account-service
#              uri: lb://account-service
#              predicates:
#                - Path=/api/v1/accounts/**

